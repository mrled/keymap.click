{
  "name": "@mrled/clickmap",
  "version": "2.0.0",
  "private": false,
  "type": "module",
  "main": "dist/webcomponent/clickmap.js",
  "scripts": {
    "lint": "eslint",
    "lintall": "eslint .",
    "component:build": "rm -rf dist/webcomponent && esbuild --bundle --sourcemap --format=esm --loader:.css=text  --global-name=KeymapDotClick --outfile=dist/webcomponent/clickmap.js src/index.ts",
    "testsite:separate": "(rm -rf dist/separate || true) && mkdir -p dist/separate && npm run component:build && cp -r dist/webcomponent/clickmap.js* dist/separate/ && cp sites/separate/* dist/separate/ && node scripts/nodeserver.cjs dist/separate",
    "testsite:simple": "esbuild --bundle --watch --outfile=sites/simple/clickmap.js --format=esm --sourcemap --loader:.css=text --servedir=sites/simple/ src/index.ts",
    "website:dev": "vite sites/keymap.click",
    "website:build": "vite build --emptyOutDir --outDir ../../dist/keymap.click sites/keymap.click",
    "website:preview": "vite preview sites/keymap.click"
  },
  "dependencies": {
    "fathom-client": "^3.0.0",
    "loglevel": "^1.7.0",
    "vite": "^5.1.5",
    "vite-tsconfig-paths": "^4.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.28",
    "esbuild": "^0.20.2",
    "eslint": "^7.9.0",
    "prettier": "2.1.1",
    "typescript": "^5.4.2"
  }
}
